#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <cmath>
#include <climits>
#include <algorithm>
#include <iomanip>

using namespace std;
using ll = long long;

vector<vector<char>> p1(4, vector<char>(4)), p2(4, vector<char>(4)), p3(4, vector<char>(4));

void rotate()
{
    
}

void move()
{

}

int main()
{
    for (auto &row : p1)
    {
        for (auto &x : row)
        {
            cin >> x;
        }
    }
    for (auto &row : p2)
    {
        for (auto &x : row)
        {
            cin >> x;
        }
    }
    for (auto &row : p3)
    {
        for (auto &x : row)
        {
            cin >> x;
        }
    }


}