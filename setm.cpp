#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <cmath>
#include <climits>
#include <map>
using namespace std;

long long n, m, p;
vector<long long> a, b;

int main()
{
    cin >> n >> m >> p;
    for (int i = 0; i < n; i ++)
        cin >> a[i];
    for (int i = 0; i < m; i ++)
        cin >> b[i];

    
}